<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP only to the local dev server (reached over `adb reverse`
  or the 10.0.2.2 emulator alias). Everything else still requires HTTPS.
  Remove this once the client's real API has a production HTTPS host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
